databricks_platform_poc:
  outputs:
    dev:
      type: databricks
      catalog: main
      schema: poc_fintech
      host: "{{ env_var('DATABRICKS_HOST') }}"
      http_path: "{{ env_var('DATABRICKS_HTTP_PATH') }}"
      token: "{{ env_var('DATABRICKS_TOKEN') }}"
      threads: 4
  target: dev
