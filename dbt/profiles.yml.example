databricks_platform:
  outputs:
    dev:
      type: databricks
      catalog: main
      schema: fintech_platform
      host: "{{ env_var('DATABRICKS_HOST') }}"
      http_path: "{{ env_var('DATABRICKS_HTTP_PATH') }}"
      token: "{{ env_var('DATABRICKS_TOKEN') }}"
      threads: 4
  target: dev
