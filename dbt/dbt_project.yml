name: databricks_platform_poc
version: 1.0.0
config-version: 2

profile: databricks_platform_poc

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  databricks_platform_poc:
    staging:
      +materialized: view
      +schema: poc_fintech
    marts:
      +materialized: table
      +schema: poc_fintech

seeds:
  databricks_platform_poc:
    +schema: poc_fintech
